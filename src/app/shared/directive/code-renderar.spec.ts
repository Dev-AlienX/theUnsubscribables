import { CodeRenderar } from './code-renderar';

describe('CodeRenderar', () => {
  it('should create an instance', () => {
    const directive = new CodeRenderar();
    expect(directive).toBeTruthy();
  });
});
