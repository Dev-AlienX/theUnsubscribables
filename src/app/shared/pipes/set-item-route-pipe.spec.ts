import { SetItemRoutePipe } from './set-item-route-pipe';

describe('SetItemRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new SetItemRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
