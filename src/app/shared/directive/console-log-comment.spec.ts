import { ConsoleLogComment } from './console-log-comment';

describe('ConsoleLogComment', () => {
  it('should create an instance', () => {
    const directive = new ConsoleLogComment();
    expect(directive).toBeTruthy();
  });
});
